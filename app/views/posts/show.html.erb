<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:page_title) %></title>
  <style>
    body {
      background: linear-gradient(to bottom, #000000, #320b69);
      color: white;
      padding: 20px;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
    }

    .actions {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
    }

    .actions strong {
      font-weight: bold;
    }

    .actions a.button {
      display: inline-block;
      padding: 10px 20px;
      text-decoration: none;
      color: white;
      background-color: #e74c3c;
      border-radius: 4px;
      transition: background-color 0.3s ease;
    }

    .actions a.button:hover {
      background-color: #c0392b;
    }

    .content {
      margin-bottom: 20px;
    }

    .comments {
      margin-top: 40px;
    }

    .comments h2 {
      margin-bottom: 20px;
      font-size: 1.2em;
      font-weight: bold;
    }

    .comment-form {
      margin-top: 20px;
    }

    .comment-form h3 {
      margin-bottom: 10px;
      font-size: 1.5em;
      font-weight: bold;
    }

    .comment-form hr {
      margin-top: 20px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="actions">
      <strong>Actions</strong>
      <div>
        <%= link_to "Edit", edit_post_path(@post), class: "button" %>
        <%= link_to "Delete", post_path(@post), method: :delete, data: { confirm: "Are you sure?" }, remote: true, class: "button is-danger" %>
      </div>
    </div>

    <hr />

    <div class="content">
      <%= @post.content %>
    </div>
  </div>

  <div class="container">
    <section class="comments">
      <h2><strong><%= @post.comments.count %></strong> Comments</h2>
      <%= render @post.comments %>

      <div class="comment-form">
        <hr />
        <h3>Leave a Reply</h3>
        <%= render 'comments/form' %>
      </div>
    </section>
  </div>
</body>
</html>
